/*class managerProduit {
    constructor(listProduit){
        this.listProduit = listProduit;
        }

        sort(){ 
           return this.listProduit.sort((a,b) =>{
               return (Date.parse(a.publicationDate) < Date.parse(b.publicationDate))?1:-1;

            });
        }
}*/